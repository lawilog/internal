
1 /* Maxima function to filter (select) elements from a list, that fulfill a specified condition */ $

filter(L, cond, var) := sublist(L, lambda([_e], subst(_e, var, cond))) $

filter([1,2,3,4,5], n>3, n);

